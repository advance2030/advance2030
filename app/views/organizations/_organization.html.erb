<tr>
  <td><%= link_to organization.name, organization_path(organization) %></td>
  <td><%= link_to 'Edit', edit_organization_path(organization) %> <%= link_to 'Delete', organization_path(organization), :method => :delete, :confirm => 'Are you sure you want to remove?' %></td>
</tr>